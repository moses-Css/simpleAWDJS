<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title><%= page.title %></title>

  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: { primary: '#7c3aed', accent: '#06b6d4' },
          fontFamily: { sans: ['Inter', 'ui-sans-serif', 'system-ui'] }
        }
      }
    }
  </script>

  <style>
    /* small tasteful animation */
    .hero-fade { transform: translateY(8px); opacity: 0; animation: in 600ms cubic-bezier(.2,.9,.2,1) forwards; }
    .hero-fade.delay { animation-delay: 140ms; }
    @keyframes in { to { transform: translateY(0); opacity: 1; } }

    /* device pill subtle */
    .device-pill { font-variant: all-small-caps; letter-spacing: 0.02em; }
  </style>
</head>
<body class="min-h-screen bg-gradient-to-br from-white via-gray-50 to-gray-100 font-sans text-gray-900">

  <!-- CENTERED HERO (single section, polished) -->
  <main class="min-h-screen flex items-center justify-center px-6">
    <section class="w-full max-w-4xl">
      <div class="relative bg-white/80 backdrop-blur-md rounded-3xl shadow-2xl border border-gray-100 overflow-hidden">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 p-10 lg:p-14 items-center">

          <!-- Left: Text -->
          <div class="space-y-6 hero-fade">
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 rounded-xl bg-gradient-to-br from-primary to-accent flex items-center justify-center text-white font-semibold shadow-sm">AS</div>
              <div class="text-sm text-gray-500">Adaptive demo</div>
              <div class="ml-auto device-pill text-xs bg-white/60 px-3 py-1 rounded-full border text-gray-700"><strong class="ml-1"><%= device %></strong></div>
            </div>

            <h1 class="text-3xl sm:text-4xl lg:text-5xl font-extrabold leading-tight tracking-tight">
              <span class="block"><%= page.headline %></span>
              <span class="block text-primary text-2xl font-medium mt-2"><%= page.sub %></span>
            </h1>

            <p class="text-gray-600 max-w-xl">
              A compact server-side adaptive hero — server reads the User-Agent and renders a tuned variant.
              Clean, fast, and easy to prototype personalized landing experiences.
            </p>

            <ul class="mt-4 grid grid-cols-1 sm:grid-cols-2 gap-3 text-gray-700">
              <% page.bullets.forEach(function(b){ %>
                <li class="flex items-start gap-3">
                  <span class="mt-1 w-3 h-3 rounded-full bg-primary/90 shadow-sm"></span>
                  <span class="text-sm"><%= b %></span>
                </li>
              <% }) %>
            </ul>
          </div>

          <!-- Right: Minimal visual / mockup -->
          <div class="hero-fade delay flex items-center justify-center">
            <div class="w-full max-w-md rounded-2xl p-6 bg-gradient-to-b from-white/90 to-gray-50 border border-gray-100 shadow-lg">
              <div class="h-56 rounded-md flex items-center">
                <!-- subtle mockup: three device bars -->
                 <h1 class="text-xl text-gray-900 uppercase font-bold"><%= device %></h1>
              </div>

              <div class="mt-4 text-xs text-gray-500 text-center">
                User Agent: <span class="break-words text-gray-700"><%= ua %></span>
              </div>
            </div>
          </div>

        </div>
      </div>
    </section>
  </main>

</body>
</html>
